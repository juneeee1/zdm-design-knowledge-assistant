# AI 协作说明

本项目的目标是形成可被人和 AI 共同使用的值得买设计规范。

## 读取顺序

1. 先读 `llms.txt`，确定问题属于设计原则、基础规范、组件、页面、交互内容还是研发测试映射。
2. 正式结论优先来自 `knowledge/`。
3. `source/card-finder/` 是扫描证据和待整理资料。可用于追溯，但未人工确认的内容不能表述为最终规范。
4. 卡片编号问题优先使用 `packages/zdm-card-knowledge/` 的查询工具。

## 判断边界

- 一个编号代表一种卡片类型；状态和变体归在该编号之下。
- 设计编号与研发编号一一对应。
- 卡片旁备注属于知识说明，不属于卡片画面。
- 页面、场景、状态、Figma Frame 是四个不同层级。
- “Figma 可追溯”不等于“已人工确认”。
- 没有明确证据时，保留“待确认”，不要凭外观补猜用途、状态或差异。
- 除明确废弃或置灰外，默认保留；长期无人确认不是删除理由。
- 修改正式知识时，保留来源、确认状态、负责人和更新时间。

## 编辑约定

- 不直接手工修改 `source/card-finder/` 中的生成资料；修改源项目后重新同步。
- 人工补充写入 `knowledge/`，并沿用 `templates/` 的字段。
- 涉及多份文档的同一事实，应确定一个权威档案，其余位置只链接，不重复维护。
- 每次发布更新 `CHANGELOG.md`，运行 `npm run validate`。

